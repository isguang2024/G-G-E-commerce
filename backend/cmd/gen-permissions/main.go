// cmd/gen-permissions parses api/openapi/openapi.yaml and derives the
// permission_keys / api_endpoints / permission_key_api_bindings seed data.
//
// Phase 0 placeholder: real implementation lands together with Phase 1
// when the workspace example domain is wired through ogen.
package main

import "fmt"

func main() {
	fmt.Println("gen-permissions: placeholder (Phase 0)")
}
