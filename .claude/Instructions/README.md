# Instructions 文档入口

该目录用于存放“执行型功能说明”和“流程说明”文档，便于协作时快速引用。

## 子目录

- `features/`：功能说明与实现要点
- `flows/`：流程说明、链路校验与执行步骤

## 维护规则

- 从 `docs/` 迁移过来的说明文档，应保持文件名稳定并同步修正索引链接。
- 若文档升级为长期真相源，应回归 `docs/` 并在 `docs/INDEX.md` 标注入口。
