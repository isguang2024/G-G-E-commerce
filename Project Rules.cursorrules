
# Project Rules

## 1. 项目定位

这不是只服务单一后台的项目，而是一套可复用的业务脚手架。

当前 `system` 模块提供未来业务模块共用的基础能力：

- 认证
- 用户与角色
- 菜单
- 功能权限
- API 注册
- 团队与租户上下文

以后新增业务时，优先复用这套基础层，不重新造一套权限和上下文体系。

## 2. 核心文档入口

业务说明不再堆在根目录，统一放在业务模块下。

先看这里：

- `backend/internal/modules/README.md`
- `backend/internal/modules/system/README.md`
- `backend/internal/modules/system/permission/README.md`
- `backend/internal/modules/system/tenant/README.md`

本文件只保留核心约束和入口索引。

## 3. 模块开发约束

### 后端

- 每个模块按统一结构组织：
  - `module.go`
  - `handler.go`
  - `service.go`
  - `repository.go`
- 依赖注入在 `module.go` 内完成
- DTO 放在 `backend/internal/api/dto/`
- 共享模型优先放在 `backend/internal/modules/system/models/model.go`

### 前端

- API 放在 `frontend/src/api/`
- 类型放在 `frontend/src/types/`
- 菜单只来自后端，不由前端静态补路由
- 按钮权限统一走 `v-action`

## 4. 权限体系核心规则

### 菜单权限

- 只控制页面入口和导航显示
- 默认由角色控制
- 用户层不单独维护一套菜单权限表
- 菜单如需跟功能权限联动，优先通过菜单元数据绑定基础功能门槛

### 功能权限

- 控制按钮和接口
- 唯一标识是：
  - `resource_code`
  - `action_code`
  - `scope_id`
- 前端可使用：
  - `resource:action`
  - `resource:action@scope`
- 后端统一使用 `RequireAction(...)`

### 数据权限

- 当前仅保留配置基础
- 默认不作为新增业务接入的第一优先级

## 5. 多租户与团队规则

- `global` 表示平台上下文
- `team` 表示当前团队上下文
- 团队业务表默认带 `tenant_id`
- 平台侧接口和 `my-team` 接口语义必须分开
- 不要让一个接口同时混跑多个作用域

如果一个业务动作同时支持平台和团队：

1. 权限定义拆成两条
2. 路由入口可以拆成两套薄入口
3. 业务实现尽量共用同一套 service

## 6. API 注册规则

所有要进入权限体系的接口，注册时都应明确：

- `resource_code`
- `action_code`
- `scope`
- `feature_kind`
- `module`

接口元数据用于：

1. API 注册表同步
2. 功能权限自动补齐

## 7. 迁移规则

- 表结构变更走 `database.AutoMigrate`
- 数据修复、历史回填、索引整理走 `backend/cmd/migrate/main.go` 的命名迁移
- 命名迁移必须幂等
- 被后续迁移完全覆盖的过渡迁移应及时清理，避免长期堆积

## 8. 新业务接入清单

新增业务模块时，至少补齐：

1. 模块目录与标准文件
2. 数据表和 `AutoMigrate`
3. 菜单入口
4. 功能权限定义
5. API 注册元数据
6. 团队数据隔离

## 9. 文档维护规则

- 根目录不再持续堆放阶段性设计说明
- 说明文档写在对应业务模块下
- 只保留长期稳定、能指导后续开发的重点内容
