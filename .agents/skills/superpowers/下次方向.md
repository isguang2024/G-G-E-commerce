# 下次方向

- 新开 Codex 会话或重启应用后，再确认当前仓库能正常发现 `superpowers` 技能。
- 如果后续希望所有仓库都能使用这套技能，再考虑同步到用户级技能目录，而不是只保留项目级接入。
- 继续观察收轻后的触发效果，重点看 `using-superpowers`、`brainstorming` 是否还会对中小任务造成额外流程负担。
- 继续观察 `subagent-driven-development` 的轻量 review 深度是否足够；如果仍然偏重，再把小任务的默认 reviewer 链进一步收窄。
- 如果还想继续减轻，下一步优先评估是否直接停用 `brainstorming` 或 `writing-plans`，把仓库主线进一步收成“调试 + 验证 + 必要 review + 并行子代理”。
