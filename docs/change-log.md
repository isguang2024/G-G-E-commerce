# 变更日志

> 本文件只保留当前架构仍有长期参考价值的近期里程碑；同日过程记录已合并进专题文档，不再继续保留。

## 2026-03-24 权限、API 与页面主链定版

### 本次改动

- 权限主数据正式收口到 `permission_keys`，功能包、快照、平台/团队双上下文进入统一主链。
- API 元数据改为代码注册优先，页面管理也从设计稿进入正式实现，`ui_pages`、未注册页面同步和公开页访问链路均已落地。
- 权限、API、页面三条管理链开始使用统一语义，不再继续扩旧模型。

### 下次方向

- 持续压缩旧命名和旧表述，补运行时读链与管理页回归。
- 后续新增系统能力时，继续优先进入现有主链，不再平行扩散。

## 2026-03-26 菜单体系、运行时读链与后端一致性收口

### 本次改动

- 菜单体系去掉“内页菜单”历史模型，菜单回归导航职责；菜单管理分组升级为独立表。
- 运行时页面与菜单读链完成瘦身，菜单树、页面注册表和公开页读取都改成轻量输出。
- 后端同步补齐鉴权、事务一致性、快照刷新、缓存失效和性能热点收口，数据库升级到 PostgreSQL 18.3。

### 下次方向

- 继续围绕运行时读链补自动化与集成测试。
- 保持菜单、页面、权限三者分层明确，不再回退到混合模型。

## 2026-03-27 文档与 API 注册体系收敛

### 本次改动

- 文档体系改为“专题文档 + 少量里程碑”结构，`docs/README.md` 收成正式入口。
- API 自动注册改为固定 GUID 查缺，不再按 `method + path` 覆盖已有记录；补齐失效标记、失效清理与分类回填。
- 后续架构结论优先回写专题文档，变更日志只保留长期参考价值的节点。

### 下次方向

- 继续把“自动同步”和“人工维护”边界压清楚，避免运行时再次反向覆盖管理数据。
- 对未注册、失效、分类诊断继续保持统一主链语义。

## 2026-03-29 系统管理、消息系统与菜单-页面关系成型

### 本次改动

- 系统管理界面完成一次大收口：壳层、顶部、用户/菜单/API/页面管理页统一到更稳定的管理页结构。
- 消息系统从设计进入真实实现，形成“消息发送、模板、发送人、接收组、发送记录、消息中心”主链，并收口为摘要纯文本、正文富文本、跳转依赖正文链接。
- 菜单与页面关系从隐含逻辑改成后台可见、可挂接、可预览，个人中心也改为正式页面注册，不再作为隐藏菜单长期漂浮。

### 下次方向

- 继续补系统收尾和演示数据，不再扩新的系统页能力。
- 后续进入业务模块时，用消息、菜单、页面、权限四条链直接承压。

## 2026-03-30 空间化底座、可配置多 Host 与系统最终收口

### 本次改动

- 菜单空间与可配置多 Host 底座正式落地：默认继续走 `default` 单域单菜单，新增空间、默认首页、Host 绑定、主绑定唯一、准入规则、复制默认菜单、重新初始化与静默回退全部进入同一模型。
- 菜单、页面、运行时页面注册表、消息正文内链、高频入口跳转都接入空间解析；运行时页面改成“服务端先按用户/团队/空间/访问模式裁剪，前端再兜底”。
- 消息系统完成异步投递、发送记录详情审计、富文本内链统一跳转，团队消息入口与平台消息入口保持分离但结构一致。
- 系统部分最终收口完成：补齐 [system-wrapup-plan.md](/C:/Users/Administrator/Documents/GitHub/G-G-E-commerce/docs/system-wrapup-plan.md)、[system-regression-checklist.md](/C:/Users/Administrator/Documents/GitHub/G-G-E-commerce/docs/system-regression-checklist.md)、[system-demo-data.md](/C:/Users/Administrator/Documents/GitHub/G-G-E-commerce/docs/system-demo-data.md)、[system-compatibility-audit.md](/C:/Users/Administrator/Documents/GitHub/G-G-E-commerce/docs/system-compatibility-audit.md)，并固定 `go run ./cmd/init-demo` 作为非生产演示数据初始化命令。
- 最终回归已覆盖平台管理员、团队管理员、普通成员三类账号；普通成员真实浏览器登录后会稳定落到 `#/workspace/inbox`，`/api/v1/pages/runtime` 也不再返回其无权访问的平台/团队发信页。

### 下次方向

- 短期内不再扩新的系统页能力，下一步应直接进入真实业务模块，用现有底座承载业务并暴露下一轮真正需要补的点。
- 若后续准备启用真实多 Host，再继续补登录回跳、请求基址与 Cookie 域策略；在此之前保持单域单菜单作为唯一默认模式。

## 2026-03-30 前端角色链退出菜单主链

### 本次改动

- 菜单管理弹窗已移除“角色权限”配置项，菜单保存链路也不再写入 `meta.roles`。
- 运行时菜单树归一化不再回填 `meta.roles`，前端全局指令注册中也已移除 `v-roles` 的默认安装。
- 当前前端正式权限链继续只保留 `action / permissionKey / accessMode`，不再把前端角色名当作正式鉴权配置入口。

### 下次方向

- 如果后面确认仓库内已无真实使用场景，可以继续删除 `frontend/src/directives/core/roles.ts` 和相关类型残留。
- 继续避免在前端重新引入“可配置但不生效”的角色裁剪入口，保持权限判断统一走后端与 action 链路。

## 2026-03-30 运行时页面公开页与团队上下文收口

### 本次改动

- 登录态 `/api/v1/pages/runtime` 现在会合并公开页与当前可访问页，避免游客可进但登录后丢失公开页。
- `my-team` 系列接口不再隐式猜团队，必须显式提供 `tenant_id / X-Tenant-ID / tenant_id context` 才会返回团队信息。
- 回归清单已补入运行时公开页与团队上下文一致性两条结果。

### 下次方向

- 如果后续需要“默认团队”体验，应在前端显式选择或通过新接口返回推荐团队，避免再回退到隐式猜团队。
- 保持 runtime pages 仍以服务端过滤为主，前端仅保留防御性兜底。

## 2026-03-30 前端角色指令残留清理

### 本次改动

- 删除 `frontend/src/directives/core/roles.ts`，彻底移除 `v-roles` 指令实现。
- 前端权限链路保持只依赖后端鉴权结果与 `action / permissionKey / accessMode`，不再保留可误用的角色裁剪入口。

### 下次方向

- 如需“默认团队体验”，请新增显式推荐团队/最近团队接口或前端选择流程，避免回到隐式猜团队。
- 继续保持前端权限只读化，不再引入前端角色名裁剪配置。

## 2026-03-30 菜单权限配置统一按空间过滤

### 本次改动

- 菜单权限相关 6 个配置弹窗统一改为按当前菜单空间加载菜单树，不再默认拉全局菜单树。
- 涉及功能包绑定菜单、角色菜单权限、角色权限综合配置、用户菜单裁剪、团队菜单边界、团队角色菜单裁剪。
- 保存接口与数据结构不变，仍按 `menu_ids` 保存；只收紧了“候选菜单来源”。

### 下次方向

- 后续可补“当前空间”显式提示文案，减少配置人员误判当前编辑空间。
- 如需跨空间复制菜单权限，建议单独做“复制策略”功能，不要在单个弹窗里混合多空间菜单。

## 2026-03-31 单空间运行模式与默认菜单兜底基线

### 本次改动

- 空间解析新增 `GG_SPACE_MODE`，默认 `single`；运行时菜单与页面注册在单空间模式下统一回到 `default`，保留 Host 绑定配置能力但不参与运行时分流。
- `init-demo` 调整为“双空间基线”：`ops` 每次强制复制 `default` 全量菜单配置作为管理员配置空间；`default` 收敛为最小基础菜单/页面（Dashboard、Console、WorkspaceInbox 与用户中心/消息中心）。
- `docs/system-demo-data.md` 同步更新单空间模式、管理员空间复制策略和默认空间最小基线规则。

### 下次方向

- 若后续启用真实多 Host 分流，再把 `GG_SPACE_MODE` 切到 `multi` 并补登录回跳、Cookie 域与 Host 绑定联调清单。
- 默认空间继续保持最小兜底，不再回填全量系统菜单，避免空间策略与业务菜单耦合。

## 2026-03-31 单空间模式补丁：恢复显式进入管理空间

### 本次改动

- 修复 `single` 模式过度收敛问题：不再无条件强制所有请求回 `default`。
- 新行为改为“禁用 Host 自动分流，但保留显式 `space_key` 切空间能力”，包括菜单树解析、当前空间解析和 runtime pages 解析。
- 结果是平台管理员可继续显式进入 `ops` 等管理空间；普通未显式指定时仍走 `default` 最小兜底。

### 下次方向

- 如后续保留隐藏式空间切换，可补一个仅管理员可见的“当前空间入口”提示，降低误判“空间不可进入”的成本。
- 持续保持 `single` 与 `multi` 两种模式行为边界清晰：`single` 禁 Host 自动分流，`multi` 启用 Host/显式空间共同解析。

## 2026-03-31 空间参数透传修复：解决 `space_key=ops` 仍 404

### 本次改动

- 修复前端路由注册链路未稳定透传空间参数的问题：菜单树请求与运行时页面注册请求统一按当前空间携带 `space_key`。
- 修复后，访问 `#/system/menu?space_key=ops` 时会以 `ops` 空间拉取菜单和页面注册，不再误用 `default` 结果导致 404。
- 调整点覆盖菜单处理器、系统 API 封装与路由守卫；`pnpm exec vue-tsc --noEmit` 已通过。

### 下次方向

- 建议补一条端到端回归：同账号在 `default` 与 `ops` 间切换时，菜单树、runtime pages 与目标页面准入保持一致。
- 如后续继续收口，可在请求层统一注入 `space_key`（或 `X-Space-Key`）策略，减少调用处重复传参。

## 2026-03-31 默认空间菜单恢复为全量

### 本次改动

- 回退 `init-demo` 中对 `default` 空间的最小化裁剪逻辑，不再删除默认空间菜单和页面。
- `init-demo` 继续保留 `ops` 初始化（从 `default` 复制）能力，但默认空间改回全量系统菜单，便于本地调试直接进入管理页。
- 文档 `system-demo-data.md` 已同步为“default 全量基线”。

### 下次方向

- 若后续再次引入“最小兜底”策略，建议改为可配置开关（如 `--shrink-default`），避免影响日常调试入口。
- 在恢复默认全量期间，继续通过权限裁剪控制普通成员可见范围，不靠菜单删除实现隔离。
## 2026-03-31 团队管理默认注册与菜单补齐

### 本次改动

- 为 `/team/team` 补充默认页面种子 `team.team`，并把页面挂到新增菜单 `TeamManage`，避免继续出现在“未注册页面”候选列表。
- 新增“团队管理 -> 所有团队”默认菜单种子，同时把 `tenant.manage` 和相关菜单绑定补入 `platform.system_admin`，让平台管理员初始化后即可看到入口。
- 新增命名迁移 `20260331_team_manage_menu_binding_fix`，迁移时会同步菜单、页面、功能包绑定并刷新默认访问快照；已验证：`go test ./...`、`pnpm exec vue-tsc --noEmit`。

### 下次方向

- 执行一次实际库迁移并回归确认：现网/本地已有库中 `TeamManage` 菜单、`team.team` 页面记录和平台管理员侧边栏是否都已自动补齐。
- 继续梳理 `TeamRoot` 下平台页与团队页的边界，避免后续再把平台管理入口和团队上下文入口混挂在同一分组下。

## 2026-03-31 菜单空间模式改为前端可配置（默认 single）

### 本次改动

- 移除菜单空间运行模式对环境变量 `GG_SPACE_MODE` 的依赖，改为从 `system_settings` 读取键 `system.menu_space.mode`；读取失败或不存在时默认回落 `single`。
- 后端新增模式管理接口：`GET /api/v1/system/menu-space-mode`、`PUT /api/v1/system/menu-space-mode`，并接入 `system.menu.manage` 权限。
- 前端菜单空间页新增“单空间/多空间”显式切换与保存按钮，加载模式失败时前端同样回落 `single`，并继续展示解析来源与请求 Host。
- 影响范围覆盖空间解析链路与运行时页面空间解析（space 模块、page runtime、菜单空间管理页与系统 API 封装）。
- 验证：`go test ./internal/modules/system/space ./internal/modules/system/page ./internal/modules/system/system` 通过；前端全量 `tsc` 未执行完成（仓库存在既有缺失文件导致类型检查失败，非本次改动引入）。

### 下次方向

- 建议补一个“模式切换影响说明”提示（如 single 不做 Host 自动分流、multi 启用 Host 绑定），降低误操作。
- 建议补充接口级回归：切换模式后 `/menu-spaces/current` 的 `resolved_by` 与实际 Host 命中行为一致。
- 如需更稳妥，可增加模式变更审计日志（记录操作人、旧值、新值、时间）。
## 2026-03-31 团队菜单重复树清理

### 本次改动

- 新增迁移 `20260331_team_root_duplicate_cleanup`，自动收敛重复的 `TeamRoot` 菜单树，保留带 `TeamManage` 的主树作为规范入口。
- 清理时会把旧树上的页面父菜单引用、功能包菜单绑定以及相关菜单边界引用迁移到主树对应菜单，避免直接删除时报“存在子菜单/仍有关联”。
- 已执行迁移并核对数据库：当前仅保留一棵 `TeamRoot`，`team.team` 和团队消息相关页面都已挂到正确菜单；已验证 `go test ./...`。

### 下次方向

- 建议前端刷新一次菜单管理页和侧边栏，确认重复“团队管理”已消失，并验证“所有团队”入口可正常打开。
- 后续若还有类似“同名根菜单重复”问题，可把这套收敛逻辑抽成通用菜单去重迁移，减少手工排库。

## 2026-03-31 未启用菜单仍显示修复

### 本次改动

- 修复运行时菜单序列化丢失 `meta.isEnable=false` 的问题：后端 `menuToRuntimeMap` 现在会保留布尔值本身，不再只回传 `true`。
- 前端菜单处理器新增禁用菜单兜底过滤；即使后端以后再遗漏一次，侧边栏也不会继续把 `未启用` 菜单当成启用展示。
- 新增后端单测覆盖 `isEnable=false` 场景；已验证 `go test ./internal/modules/system/menu`、`pnpm exec vue-tsc --noEmit`。

### 下次方向

- 建议再回归一次“菜单管理页改成未启用 -> 刷新侧边栏/重新登录”的完整链路，确认菜单展示与状态列完全一致。
- 如果后续还要区分“禁用但管理端可见”和“运行时完全隐藏”，可以把当前 `meta.isEnable` 语义单独文档化，避免状态含义再次漂移。
