# 变更日志

> 本文件只保留当前架构仍有参考价值的近期里程碑。更早、更细的过程记录已合并进专题文档，不再继续保留。

## 2026-03-24 权限主链与快照体系定版

### 本次改动
- 权限主数据正式收口到 `permission_keys`，功能包与权限键关系收口到 `feature_package_keys`。
- 平台用户、平台角色、团队边界、团队角色四条运行时读链统一以快照表为主，不再依赖旧正向权限表。
- 平台与团队双上下文、功能包 `base/bundle`、减法裁剪和刷新入口形成统一主线。

### 下次方向
- 继续把专题文档和前端页面说明压到统一语义，减少旧术语残留。
- 对快照刷新和运行时读链补更稳定的自动化覆盖。

## 2026-03-24 API 元数据与权限体系收口

### 本次改动
- API 元数据继续以代码注册为主入口，落库到 `api_endpoints` 并通过 `api_endpoint_permission_bindings` 绑定权限键。
- API 管理页补齐未注册接口补录、分类化查询和状态展示，正式进入权限主链。
- 旧 `permission_action` 命名与旧字段逐步退出正式模型。

### 下次方向
- 继续把 API 运行时状态、权限来源与管理页诊断能力做得更闭环。
- 保持 API、权限键、功能包三者语义一致，避免再次分叉。

## 2026-03-25 页面管理链路收口

### 本次改动
- 页面管理从“方案”进入正式实现：`ui_pages`、未注册页面同步、路径预览、挂载方式、公开页访问链路已落地。
- 页面类型收口为 `inner / global / group / display_group`，页面运行时注册与管理视图分层明确。
- 公开页面未登录访问链路、父级路径拼接、分组与页面拆分均已接入正式前后端主链。

### 下次方向
- 继续压缩页面管理文档中的历史方案口吻，只保留当前实现和剩余边界。
- 对页面管理链路补更稳定的自动化验证。

## 2026-03-26 菜单体系去内页与分组独立

### 本次改动
- 菜单“内页类型”退出正式主链，菜单回归导航入口职责。
- 菜单管理分组升级为独立表 `menu_manage_groups`，菜单管理页按分组组织，但不影响运行时权限和路由。
- 配置型大弹窗统一收口到右侧抽屉，管理页交互基线更加一致。

### 下次方向
- 持续清理菜单与页面文档中的旧“内页菜单”表述。
- 对菜单分组、备份恢复、页面父菜单联动补回归验证。

## 2026-03-26 运行时页面与菜单读链瘦身

### 本次改动
- 运行时菜单树按默认值瘦身，运行时页面接口按轻量字段输出。
- 页面运行时注册表补缓存与公开页轻量接口，读取阶段已拍平分组链。
- 用户权限测试、API 停用缓存、团队链路诊断等运行时辅助能力已补齐。

### 下次方向
- 继续评估是否需要按路径懒匹配页面运行时结果。
- 为运行时菜单与页面读链补更完整的冒烟与自动化覆盖。

## 2026-03-26 PostgreSQL 16 升级到 18.3

### 本次改动
- Docker 中的 PostgreSQL 已从 16 升级到 18.3，并切到新数据卷。
- 业务库通过逻辑备份恢复到新实例，旧卷与备份在确认后已清理。
- Compose 文件中的过时 `version` 字段已删除。

### 下次方向
- 如需继续做数据库层演进，保持“新卷迁移 + 验证 + 再清理回退资源”的流程。
- 补应用层接口级连库回归时，优先覆盖迁移、初始化和扩展相关链路。

## 2026-03-26 后端权限链路与一致性全量修复

### 本次改动
- 补齐 API Key 真实校验、CORS 回显源站、启动期配置校验、HTTP 超时、数据库与 Redis Ping 超时，并按环境收敛 Gorm 日志级别。
- 修正权限链路中的 `RequireAnyAction`、权限键改名联动、平台角色重复刷新；用户更新与团队成员变更改为事务内完成，避免 `tenant_members`、`user_roles`、用户角色快照出现半成功状态。
- 菜单恢复与更新流程补了关联清理和全量快照刷新，平台/平台角色快照的功能包展开改成批量图遍历，并为公共菜单读取加了最小缓存与缓存失效。
- 已验证：`go test ./...` 通过；MiniMax 外部审查尝试执行，但请求在远端接口 60 秒读超时，未获得结果。

### 下次方向
- 补集成测试，优先覆盖 API Key 认证、权限键改名、团队成员角色切换、菜单备份恢复后的快照刷新。
- 继续收敛平台侧按角色循环取菜单来源的查询，必要时再把角色维度菜单聚合改成一次批量装载。
- 检查历史环境中的 API Key `key_hash` 生成规则；当前兼容了哈希值与明文回退查询，但仍建议统一为单一哈希策略并补迁移脚本。

## 2026-03-26 后端全量修复复核收口

### 本次改动
- 完成后端 18 个核心文件收口，覆盖 API Key 鉴权、CORS、配置校验、HTTP 超时、数据库/Redis 连接探测、权限联动刷新、用户与团队事务一致性、菜单恢复后的关联清理与快照刷新。
- 权限侧补齐 `RequireAnyAction` 多权限放行语义、`permission_key` 改名时同步 `api_endpoint_permission_bindings`、`RefreshPlatformRole` 去重；平台和平台角色快照改为批量装载功能包图，并对公共菜单查询加了轻量缓存。
- 已验证：`go test ./...` 通过。未验证：真实数据库中的历史 API Key `key_hash` 是否全部与当前 SHA-256 规则一致，原因：本轮未接入生产/历史数据做实库抽样。

### 下次方向
- 补后端集成测试，优先覆盖 API Key 认证、用户角色更新事务、团队成员角色切换、菜单备份恢复后的快照重建。
- 如历史环境存在非当前规则的 API Key 哈希，补一轮迁移脚本或兼容策略，并在上线前抽样校验。
- 继续观察平台访问快照在大角色集下的查询成本，必要时把角色维度菜单来源合并为一次批量查询。

## 2026-03-27 文档体系收敛为最新架构说明

### 本次改动
- 重写 `PROJECT_FRAMEWORK.md`、`FRONTEND_GUIDELINE.md`、`docs/README.md`、权限总览、菜单页面文档，清空过时方案和历史 append 口吻。
- `change-log.md` 从 100+ 条碎片记录裁剪为少量近期里程碑，长期结论回写到专题文档。
- 文档基线统一到当前代码现状：权限快照主链、菜单/页面分层、运行时页面注册、API 元数据与状态控制。
- 未验证：`go test ./...`、`pnpm exec vue-tsc --noEmit`，原因：本轮仅修改文档，未触发代码与构建链路变更。

### 下次方向
- 后续新增架构调整时，优先改专题文档，再决定是否记录到日志。
- 如果某个模块再次出现“方案文档”和“现状文档”并存，应尽快合并，避免文档重新发散。

## 2026-03-26 后端全量修复最终验证补录

### 本次改动
- 收口后端鉴权、权限联动、团队与用户事务一致性、菜单恢复清理、平台快照刷新与热点查询性能问题，最终落地到 20 个后端核心文件。
- API Key 改成真实数据库校验并补过期/租户检查；权限键改名会同步 API 绑定并触发快照刷新；菜单增删改与备份恢复统一补了缓存失效和全量快照重建。
- 平台用户与平台角色的公开菜单判定统一兼容 `accessMode=public/jwt` 及历史布尔标记；平台用户菜单快照的角色贡献计算改成一次批量装载角色包、功能包图和隐藏菜单，去掉按角色循环查库。
- 已验证：`go test ./...` 通过。

### 下次方向
- 优先补 API Key、权限键改名、团队成员角色切换、菜单备份恢复这几条高风险链路的集成测试。
- 未验证：历史环境 `api_keys.key_hash` 是否全部遵循当前 SHA-256 规则，原因：本轮未接入真实历史库做抽样校验。

## 2026-03-27 API 自动注册改为仅补缺不覆盖

### 本次改动
- 启动期 `apiregistry.SyncRoutes` 改成 insert-only：仅当 `api_endpoints` 中不存在对应路由时才新增，已有记录不再覆盖分类、功能归属、权限键、说明、来源、团队上下文和停用状态。
- API 管理页的人工编辑现在会在重启后保留，不会再被自动注册回写冲掉；已补单测锁定“已有记录不覆盖”的同步策略。
- 已验证：`go test ./...` 通过。

### 下次方向
- 当前策略会保留历史记录，如果代码里改了路由 path 或删除路由，旧 API 注册项不会自动清理；后续可补“失效路由识别/清理”能力。
- 如果未来还需要“代码元数据同步”和“人工编辑保留”两者兼得，建议再引入更细的字段级覆盖策略或显式同步按钮。

## 2026-03-27 API 固定 GUID 自动注册收口

### 本次改动
- API 自动注册改成按源码固定 GUID 查缺：`apiregistry` 增加了固定 GUID 注册表，走 `apiregistry.Registrar` 的系统 API 在源码里都有明确 `code`，启动时仅按该 GUID 判断是否缺失，不再按 `method+path` 覆盖已有记录。
- 认证、媒体接口也接入了注册器；若后续新增系统 API 但未分配固定 GUID，启动同步会直接跳过该路由，避免再次回退成按路径派生新标识。
- API 管理保存时改为保留原有固定 GUID，避免编辑后重新计算 `code`；“未注册 API”列表也同步改成优先按 GUID 判定，避免数据库里手工改过路径后被误判成新接口。
- API 状态拦截增加对 `/api/v1/api-endpoints` 管理路由的绕过，修复“把 API 停用后连管理页也一起锁死”的问题。
- 已验证：`go test ./...` 通过。

### 下次方向
- 目前固定 GUID 自动覆盖的是走 `apiregistry.Registrar` 的接口；如果后续要把原始 `gin` 直注册路由也纳入自动注册，需要统一接入同一套注册器或显式补固定 GUID。
- 现在自动注册只负责补缺，不负责删除源码里已经移除的旧记录；如果要做清理，建议后续单独补“失效 API 标记/回收”流程，避免误删人工维护数据。

## 2026-03-27 API 失效标记与清理收口

### 本次改动
- 后端为 API 管理列表补了运行态识别：接口列表现在会返回 `runtime_exists`、`stale`、`stale_reason`，仅把“来源为自动同步、运行时已不存在”的接口标记为失效，不影响手工维护 API。
- 新增 `POST /api/v1/api-endpoints/cleanup-stale`，会事务内清理失效 API 的权限绑定并软删除失效记录，然后刷新 API 运行时状态缓存。
- 前端 API 管理页新增“清理失效 API”入口，状态列对失效接口优先显示“失效”，并统计失效数量；已验证：`go test ./...`、`pnpm exec vue-tsc --noEmit` 通过。

### 下次方向
- 建议再补一轮实机联调，确认“清理失效 API”在真实数据下的提示文案、分页刷新和未注册 API 统计符合预期。
- 若后续有源码删除接口的场景，新增/移除固定 GUID 时继续保持台账一致，避免历史自动同步接口长期沉积。

## 2026-03-27 API 分类回填与注册器分类兜底

### 本次改动
- `apiregistry.Registrar` 恢复使用第二个参数作为默认分类提示，同一注册器下未显式 `BindGroup/BindCategory` 的接口会自动继承分类；`GETAction/POSTAction` 这类快捷注册也统一走这套逻辑。
- 自动注册同步新增“仅回填空分类”策略：已有 API 记录如果 `category_id` 为空，会按当前源码分类补齐，但不会覆盖已手工维护的分类。
- API 分类种子新增 `media`，并补齐了团队模块直接手写 `RouteMeta{}` 的公开接口分类。已验证：`go test ./...` 通过；数据库 `uncategorized_count = 0`；浏览器实测 `API管理` 分类树已恢复正常。

### 下次方向
- 后续新增模块时，仍建议优先使用 `reg.Meta(...).BindGroup(...)` 或至少保证注册器分类提示正确，避免再出现“能注册但无分类”的灰色状态。
- 如果以后允许“故意留空分类”，需要再补一个显式开关；当前策略会把自动同步 API 的空分类视为待修复数据并自动补齐。

## 2026-03-28 后台管理界面成熟度差距分析补充

### 本次改动
- 基于真实浏览器实测和当前前端实现，补了一份“当前后台和成熟管理系统差距在哪里”的专项评估，并把结论回收到后续界面收口优先级里。
- 文档明确收口了本轮范围：只讨论后台壳层、菜单、页面、用户、API 管理体验，不把多域名权限纳入当前补齐范围。
- 补充了当前判断基线：页面管理可作为成熟度对齐标杆；首页壳层、用户管理、菜单管理、API 管理仍需继续收口。
- 未验证：`pnpm exec vue-tsc --noEmit`，原因：本轮仅补文档和界面评审结论，未修改前端实现。

### 下次方向
- 如果下一轮开始做界面收口，优先替换模板型首页，补平台 / 团队上下文标识，并统一搜索区、页面头部和抽屉结构。
- 先用页面管理的结构化抽屉和链路表达做基线，再逐步改菜单、用户、API 管理，不要并行扩散新的页面模式。

## 2026-03-28 API 未分类保留与失效勾选清理

### 本次改动
- 自动注册不再按权限键推断分类，也不再对空分类做启动回填；未分类 API 现在会被保留，避免人工编辑被重启覆盖。
- 失效 API 清理改为“按勾选删除”：后端 `cleanup-stale` 改为接收 `ids`，仅删除被选中且当前仍失效的记录，并联动删除绑定。
- 前端 API 管理页新增失效 API 列表弹窗，用户可多选后执行删除；不再默认一次清空全部失效 API。
- 已验证：`go test ./...`（backend）通过；`pnpm exec vue-tsc --noEmit`（frontend）通过。未验证：浏览器自动化实机流程，本地 Playwright 启动被现有 Chrome 会话占用导致失败。

### 下次方向
- 补一条前端 E2E 用例，覆盖“打开失效弹窗 -> 勾选 -> 删除 -> 刷新统计”完整链路，避免回归到全量删除行为。
- 如需进一步减轻 `loadCategorySummary` 的全量拉取开销，可单独增加后端“失效列表/统计”接口，前端改为按需拉取。

## 2026-03-29 API 管理页全量拉取收敛

### 本次改动
- 新增 `GET /api/v1/api-endpoints/overview` 和 `GET /api/v1/api-endpoints/stale`，前端分类树统计与失效弹窗改为分别走概览统计和分页列表，不再为统计信息循环拉取整张 API 表。
- `cleanup-stale` 清理链改为只扫描 `source=sync` 的运行态候选，`unregistered` 对比链改为按运行时路由定向查询可能命中的已注册 API，不再固定拉 `5000` 条注册记录做全表比对。
- API 管理页里的未注册列表默认分页改为 20 条起；失效弹窗也改为 20/50/100 分页，并支持跨页勾选后统一删除。
- 已验证：`go test ./...`（backend）通过；`pnpm exec vue-tsc --noEmit`（frontend）通过。

### 下次方向
- 当前 `overview/stale/cleanup-stale` 为了识别 stale，仍需要扫描一次 `source=sync` 的候选接口；如果后续同步接口规模继续增长，可再考虑把运行态摘要缓存化。
- 继续排查其它后台管理页是否存在“前端为统计循环翻页拉全表”的旧模式，优先处理权限键、功能包、页面注册这几块。

## 2026-03-29 管理弹窗候选接口去全量收敛

### 本次改动
- 后端新增 `roles/options`、`feature-packages/options`、`tenants/options`、`permission-actions/options` 四组候选接口，选择器场景改为直接返回候选集，不再让前端用分页接口拉满整表。
- 角色筛选、角色功能包、用户功能包、团队功能包、组合包基础包、功能包团队、功能包功能范围、菜单功能权限、团队边界、API 绑定权限键等弹窗和下拉全部切到新候选接口，去掉了多处 `size:1000` 和循环翻页拉全表逻辑。
- API 管理里的权限键绑定弹窗也改成一次拉取候选接口；当前前端残留的整表拉取只剩页面管理主树，该场景仍需要完整树数据，暂未强行改成交互更重的懒加载。
- 已验证：`go test ./...`（backend）通过；`pnpm exec vue-tsc --noEmit`（frontend）通过。

### 下次方向
- 页面管理主树仍在用 `fetchGetPageList({ current: 1, size: 1000 })`；若继续深挖，可新增轻量树接口或改成按节点懒展开，但这会连带调整当前页面管理交互。
- 后端更深层的“全量刷新”还剩菜单快照刷新链路；后续可继续把 `refreshAllMenuSnapshots` 之类的重操作改成增量刷新或异步任务，进一步降低大数据量下的后台抖动。

## 2026-03-29 未注册 API 补固定编码并归类

### 本次改动
- 为 `overview`、`stale`、`roles/options`、`feature-packages/options`、`pages/options`、`permission-actions/options`、`tenants/options` 这 7 条路由补齐固定 `route code`，避免同步器因缺少固定编码跳过注册。
- 复用各模块现有的注册元数据完成回填，这 7 条已全部进入正式 `api_endpoints` 表，并分别归到 `api_endpoint`、`feature_package`、`page`、`permission_key`、`role`、`tenant` 正常分类。
- 已验证：执行同步后“未注册 API”清单从 7 条变为 0 条；`go test ./...`（backend）通过。

### 下次方向
- 当前仍依赖 `fixed_codes.go` 维护固定编码；后续可评估把缺码检测前置到测试或启动检查，避免再次出现“有元数据但未注册”的漏项。
- `tenants/options` 目前只确认了后端注册与分类，前端仓库里未检到直接消费点；若后面接入团队选择器，可直接复用这条正式注册接口。

## 2026-03-29 团队候选前端统一与阶段性文档清理

### 本次改动
- 前端 `system-manage` 新增 `fetchGetTenantOptions`，系统管理域内的团队候选接口现在和角色、页面、功能包、权限键候选保持同一出口。
- 功能包团队抽屉改为直接复用 `system-manage` 里的团队候选接口，减少系统管理页跨 `team.ts` 的历史分叉。
- 删除阶段性的 `admin-management-ui-gap-analysis.md` 索引入口，相关结论保留在规范和里程碑里，不再单独保留一次性差距评估文档。
- 已验证：前后端当前都在运行，无需重启；`go test ./...`（backend）通过。未验证：`pnpm exec vue-tsc --noEmit`，原因：本轮未重新跑前端类型检查。

### 下次方向
- 页面管理主树仍在走 `fetchGetPageList({ current: 1, size: 1000 })`，因为当前 `pages/options` 字段不足以替代正式树数据；如果要继续收缩全量拉取，需要先补轻量树接口。
- 下一轮前端收口优先做壳层首页、平台/团队作用域标识、用户管理头部摘要，再把菜单/用户/API 三个抽屉继续向页面管理的分段式结构靠齐。
